# -*- mode: snippet -*-
# contributor : mabo3n
# name: try { ... } catch (ex) { ... } finally { ... }
# key: trycf
# --
try
{
    $0
}
catch (${1:Exception} ex)
{
    ${2:Console.WriteLine(ex.ToString());}
}
finally
{
    ${3:// resource}.Dispose();
}